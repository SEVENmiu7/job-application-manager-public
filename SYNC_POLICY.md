# 上游同步与隐私策略

公开多人版与正式版、面试演示版是三个独立项目。同步只发生在代码层，不共享数据库、应用身份或用户记录。

## 可以同步

- 通用界面、交互和无个人配置的前端组件。
- 通用业务规则、时间处理、排序筛选和性能优化。
- 不扩大数据访问范围的安全修复与自动化测试。
- 经预览和确认后的独立数据库结构变更。

## 永远不应同步

- `.git`、`.env*`、`.spark`、构建产物、日志和依赖目录。
- 正式版或演示版的应用 ID、数据库记录、附件、密钥和私人链接。
- 飞书多维表格入口、Base ID、数据表 ID、手动同步代码和数据变更通知。
- 依赖创建者身份的配置、管理员读取能力或跨用户数据访问。

## 每次同步后的检查

1. 确认所有接口仍要求登录。
2. 确认所有查询和写操作都包含当前用户 ID 条件。
3. 确认数据库策略只允许本人读写，并拒绝匿名访问。
4. 确认空账号不会自动生成样例或复制上游数据。
5. 运行隐私扫描、类型检查、代码检查、单元测试和生产构建。
6. 在 `CHANGELOG.md` 记录来源提交、同步内容和排除项。
