# 更新记录

本文件记录公开多人版的功能变化、上游来源和隐私边界。

## 2026-09-15

来源：正式版提交 `527df40`、`0956e2d`、`d6609af`、`a498513`、`816fe68`、`71b0dba`、`490a9f0` 和 `0e74d69`，同步基线为 `0e74d69`。

### 已同步

- 新增求职待办中心，支持创建、编辑、搜索、重要标记、截止时间、提醒时间、完成状态和关联投递岗位。
- 新增可拖动的快捷待办入口；投递列表和看板可把“下一步安排”直接转为待办。
- 抽取通用岗位搜索选择器，并修复窄窗口下搜索面板遮挡、聚焦裁切和布局溢出问题。
- 优化桌面侧边栏折叠行为，取消悬停展开，保留固定图标栏和待办数量提示。
- Git 提交前检查改为由 Node.js 跨平台启动，Windows 不再依赖额外的 `sh` 环境。
- 公开版开发及线上数据库均已新增独立 `todos` 表、岗位外键、截止时间精度和查询索引，本次同步已发布上线。
- “导出我的数据”和“删除我的全部数据”已覆盖求职待办；Excel 新增“求职待办”工作表，JSON 备份升级为版本 2。

### 公开多人版适配与排除项

- 所有待办接口必须登录，并只从登录上下文取得用户 ID；列表、详情、关联岗位校验、更新和删除均包含本人条件。
- `todos` 启用行级安全：登录用户只能读写自己的记录，匿名访问明确拒绝。
- 新账号仍保持空白，不生成演示待办或投递记录。
- 未同步正式版的飞书多维表格、手动同步、附件托管、私人配置、真实数据、管理员查看全部数据或跨用户共享能力。


## 2026-09-13

来源：正式版提交 `346331f`、`71975bf`、`1b3021e`、`7292b6d`、`6592a13` 和 `0bb57ce`，同步基线为 `0bb57ce`。

### 已同步

- 岗位采集表单可在刷新或离开页面后恢复未提交草稿，并新增相应持久化测试。
- 投递列表支持勾选岗位并进入独立对比页，集中比较薪资、工作方式、能力匹配、主观意愿、岗位亮点和主要顾虑。
- 新增与编辑投递页面加入岗位决策字段，分区导航会随点击、聚焦和滚动位置保持一致。
- 优化岗位对比页的分区导航、固定区域、移动端布局和交互反馈。
- 个人 Excel 与 JSON 备份同步包含新增岗位对比字段。
- 开发数据库新增 7 个可为空字段，旧记录保持兼容；线上数据库尚未迁移或发布。

### 公开多人版保留与排除项

- 保留必须登录、接口用户条件、数据库本人读写策略、空账号不生成样例、个人数据导出与删除能力。
- 未同步正式版的飞书多维表格、手动同步、附件托管、私人配置、管理员查看全部数据或跨用户共享能力。
- 未复制正式版数据库记录、应用 ID、环境变量、密钥、私人链接或 Git 历史。

## 2026-09-10

### 个人数据管理

- 新增“数据与隐私”页面，集中说明数据保存位置、账号隔离范围和维护边界。
- 支持将当前账号的数据导出为 Excel，分别整理投递记录和面试复盘。
- 支持下载完整 JSON 备份，导出内容不包含用户 ID 和平台审计字段。
- 支持永久删除当前账号的全部投递及关联复盘，执行前必须输入确认文字。
- 导出与删除接口均要求登录，并只使用登录上下文中的用户 ID。
- Excel 导出会处理可能被表格软件识别为公式的用户文本，降低公式注入风险。

### 页面细节优化

- 移除主内容区顶部的公开版说明横幅，页面内容恢复与正式版一致的起始位置和留白。
- 移除侧边栏底部的账号隔离说明卡片，避免重复强调系统规则。
- 品牌区标题和副标题与正式版保持一致。
- 公开多人版不加入同步按钮或私人数据表入口，侧边栏底部仅保留外观设置。

### 公开多人版首次发布

- 基于面试演示版提交 `dabcf10` 创建独立项目，继承看板、列表、岗位采集、流程时间和面试复盘等通用功能。
- 使用新的妙搭 full-stack 应用和独立空数据库，不迁移正式版、面试演示版或个人多维表格中的任何记录。
- 移除 15 条虚构演示记录及自动初始化逻辑；新用户首次登录显示空状态。
- 将产品名称、顶部提示和说明文档统一为“求职投递管理器｜公开多人版”。
- 所有业务接口要求登录，并以登录上下文中的用户 ID 作为数据归属依据。
- 列表、详情、统计、看板、更新、删除和面试复盘均保留用户条件。
- 为 `applications` 和 `interview_reviews` 配置仅本人可读写的数据库行级安全策略，并明确拒绝匿名访问。
- 删除私人多维表格入口、手动同步能力、私人链接和个人配置。

### 永久排除项

- 正式版或面试演示版的数据库记录、附件与 Git 历史。
- `.env*`、`.spark`、数据库凭证、Token、旧应用 ID 和私人链接。
- 飞书多维表格绑定、手动同步代码、数据变更通知和创建者个人配置。
- 管理员查看用户数据、跨用户协作及共享记录能力。

### 后续同步要求

- 只同步经过验证的通用业务功能和安全修复。
- 每次同步都要复核登录保护、用户 ID 条件、数据库策略、隐私扫描和自动化测试。
- 数据库变更必须先预览差异，再单独确认并发布。
